# JWT secret for SIWE token signing.
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME

# Allowed CORS origin for the backend.
# In production this is the template frontend on Vercel.
CORS_ORIGIN=https://template.cfxdevkit.org

# Docker image tag to run (default: edge)
TAG=edge
