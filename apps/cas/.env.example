# CAS_API_KEY — Bearer token for all /api/* routes.
# Generate with: openssl rand -hex 32
CAS_API_KEY=CHANGE_ME

# Allowed CORS origin for the backend.
# In production this is the CAS frontend on Vercel.
CORS_ORIGIN=https://cas.cfxdevkit.org

# Docker image tag to run (default: latest)
TAG=latest
