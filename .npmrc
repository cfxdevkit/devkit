# Enforce public access for all @cfxdevkit/* scoped packages.
# The scope is owned by the cfxdevkit npm organization.
@cfxdevkit:registry=https://registry.npmjs.org/

# Require provenance attestations when publishing (SLSA level 2).
# Ignored on local `npm publish` but enforced by the CI release workflow
# via --provenance flag (requires id-token: write permission in Actions).
# See: https://docs.npmjs.com/generating-provenance-statements
provenance=true
