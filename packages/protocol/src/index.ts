/**
 * @cfxdevkit/protocol
 *
 * Static on-chain contract artifacts for Conflux DevKit automation contracts.
 * Provides ABIs, deployment bytecode, and deployed addresses for:
 *   - AutomationManager
 *   - SwappiPriceAdapter
 *   - PermitHandler
 *
 * Generated artifacts are kept in `generated.ts` (produced by wagmi CLI / Hardhat script).
 * This package has zero runtime dependencies.
 */

export * from './abi.js';
